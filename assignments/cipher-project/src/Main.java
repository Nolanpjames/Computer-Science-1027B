public class Main {
    /**
    Cipher algorithm
    3
    5
    1
    2
    4


    firstConverted = false;
    secondConverted = false;
    userInput = "AB";
    userInputChar1 = "A";
    userInputChar2 = "B"

    vowelCheck = "AEIOU";
    if userInput char 1 in vowelCheck:
        if char 1 is "A":
            convert to 1
        else if check all vowels:

        firstConverted = true;

        if userInput char 2 in vowelCheck:
            if char 1 is "A":
                convert to 1
            else if check all vowels:
            secondConverted = true;
            return 0;


    if firstConverted == false:
        userInputChar1
















     */
}
